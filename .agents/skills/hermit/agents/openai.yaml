interface:
  display_name: "Hermit"
  short_description: "Repo-specific patterns and workflows for hermit"
  default_prompt: "Use the hermit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true